Constructor- this is a special method used to initialize objects
A constructor is called when instance(object) of a class is called
it has the same name as the class it is created in
it can be used to set initial values of objects attributes

Buy Commodity (engage) negotiations

The Delivey System, Invoicing System, Tagging and Tracking they disappear (fix that, they should not do that), they come 
